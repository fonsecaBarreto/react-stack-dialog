{
  "name": "@react-stack-dialog/notifications",
  "version": "1.0.0",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "license": "MIT",
  "private": false,
  "scripts": {
    "copy:files": "copyfiles -u 1 src/**/*.css src/**/*.svg lib/",
    "build": "rimraf lib/* && npm run copy:files && tsc -p .",
    "build:watch": "npm run build -- --watch"
  },
  "devDependencies": {},
  "peerDependencies": {
    "react": ">=17.0.2",
    "react-dom": "^17.0.2",
    "typescript": ">=4.5.3"
  },
  "files": [
    "/lib"
  ]
}
